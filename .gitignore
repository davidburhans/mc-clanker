# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
.eggs/
*.egg-info/
dist/
build/
*.egg

# uv — uv.lock IS committed on purpose: Dockerfile.web and Dockerfile.worker
# both run `uv sync --frozen`, so a fresh clone needs the lock present to build.
# (Previously gitignored, which made images unbuildable from source — review F1.)

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
app.log

# Media/Exports
/exports/
*.wav
*.mp3
*.flac

# Gradio
.gradio/

# Cache
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Docker
.dockerignore

# Testing
.coverage
.pytest_cache/

# Database
data/*.db
data/*.db-*
.worktrees/

# Data outputs (batch generation, simulation runs)
data/
sim_data/

# Environment file (contains secrets)
.env

# Models / GGUF
*.gguf

# Claude IDE
.claude/

# Dynamic App State
instruments.json


# pi sub-agent harness scratch (transcripts, artifacts) — never commit
.pi-subagents/
